---
title: Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts)
tags:
  - devops
  - platform-engineering
  - docker-and-container-runtime-internals
  - principal-swe
parent: "[[Docker]]"
---

# 📦 Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts)

Named volumes for stateful persistence, host bind mounts for development, in-memory `tmpfs` mounts for sensitive ephemeral data, and volume drivers.

```text
Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts)
│
├── [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Engineering Standards and Invariants]]
├── [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Production Implementation Patterns]]
└── [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Failure Modes and Operational Mitigations]]
```

---

## 🗂️ Platform Blueprints & Operational Patterns

- [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Engineering Standards and Invariants]]
- [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Production Implementation Patterns]]
- [[Docker Storage Options (volumes, Bind Mounts, and Tmpfs Mounts) Failure Modes and Operational Mitigations]]

---

## 🔗 References
- ⬆️ Parent: `Docker & Container Runtime Internals`
- 📚 Module: `DevOps`

